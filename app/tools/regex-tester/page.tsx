import ToolClient from "./ToolClient";

export const metadata = {
  title: "Regex Tester | Yoryantra",
  description:
    "Test regular expressions instantly with live matching results.",
};

export default function Page() {
  return <ToolClient />;
}